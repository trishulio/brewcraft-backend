apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: brewcraft-backend
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - pathType: Prefix
        path: /api
        backend:
          service:
            name: app-svc-http
            port:
              number: {{ .Values.app.networking.port.http }}